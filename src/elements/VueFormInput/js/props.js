'use strict';
export default (
	{
		type:String,
		modelValue:String,
		placeholder:String,
		hint:String,
		error:Array | String,
		e:Array | String,
		disabled:Boolean,
		readonly:Boolean,
		maxlength:Number,
	});
