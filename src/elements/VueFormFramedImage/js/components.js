'use strict';
import VuePopupEdit from '../../../popups/VuePopupEdit/VuePopupEdit.vue';
import VueFormFile  from '../../VueFormFile/VueFormFile.vue';

export default (
	{
		VuePopupEdit,
		VueFormFile,
	});
