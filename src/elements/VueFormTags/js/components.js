'use strict';
import VuePopupHost from '../../../popups/VuePopupHost/VuePopupHost.vue';
import VueFormInput from '../../VueFormInput/VueFormInput.vue';

export default (
	{
		VuePopupHost,
		VueFormInput,
	});
