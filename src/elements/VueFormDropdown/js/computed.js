'use strict';
import optionsList     from './computed/optionsList';
import filteredOptions from './computed/filteredOptions';
import selected        from './computed/selected';
import dropdownHint    from './computed/dropdownHint';
import errorMessage    from './computed/errorMessage';

export default (
	{
		optionsList,
		filteredOptions,
		selected,
		dropdownHint,
		errorMessage,
	});
