'use strict';
import VueForm       from '../../VueForm/VueForm.vue';
import VueFormInput  from '../../../elements/VueFormInput/VueFormInput.vue';
import VueFormCheck  from '../../../elements/VueFormCheck/VueFormCheck.vue';
import VueFormSubmit from '../../../elements/VueFormSubmit/VueFormSubmit.vue';

export default (
	{
		VueForm,
		VueFormInput,
		VueFormCheck,
		VueFormSubmit,
	});
