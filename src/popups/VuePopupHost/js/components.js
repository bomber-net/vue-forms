'use strict';
import VueForm            from '../../../forms/VueForm/VueForm.vue';
import VueHelperPreloader from '../../../helpers/VueHelperPreloader/VueHelperPreloader.vue';

export default (
	{
		VueForm,
		VueHelperPreloader,
	});
